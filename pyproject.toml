[project]
name = "notion-assistant-bot"
version = "0.1.0"
description = "Personal Notion Assistant Bot - Telegram bot for calendar events and todos"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.27.0"
]

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk"
]
